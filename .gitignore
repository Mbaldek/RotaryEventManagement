#env
.env
.env.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.vercel
.claude/

# RSA jury materials (heavy PDFs, contains startup-private content)
pitch_decks/
briefings/

# Local-only secrets (admin PINs, etc.) — never commit
pin admi
secrets/
*.secret

# OAuth client secret JSONs (Google Cloud Console, Azure download)
# JAMAIS commit ces fichiers — secrets en clair dedans.
client_secret_*.json
*-oauth-client.json
google-credentials.json
azure-credentials.json

# Playwright artifacts
playwright-report/
test-results/

